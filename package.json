{
  "name": "ccgui-plugin-session-notes",
  "version": "0.1.0",
  "private": true,
  "description": "CC GUI 插件：给会话贴本地便签（右侧面板编辑 + 右键菜单直达），数据仅存本机。",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "validate": "node scripts/validate-manifest.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^6"
  }
}
